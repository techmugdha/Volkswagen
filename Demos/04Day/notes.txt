# arguments and parameters

# function overloadding syntax not alloed.

*args : Variable length Positional arguments

**kwargs : Variable length Keyword arguments

#-----------------------------------------------

1. Normal functions :  Created using def keyword
2. Lambda functions :  Create using lambda keyword, also as Anonymous functions.

Syntax: 
lambda ar_list: expression/ python statement

- how to hold lambda expression in a variable, and how to invoke it
- how to return lambda expression from a function
- how to declare lambda expression and how to invoke it on the same line without assigning any variable to it

#--------------------------------------------------

Higher Order Functions

# functional Programming language:
-: We can writed nested function
-: We can return function out of another function
-: We can hold function body into another variables
-: We can pass function names as a parameter to another function


#-----------------------------------
# Scopes
# Closures
# Decorates

#-----------------------------------

Types of scopes in Python:
:: Namespace : collection of names/ identifiers. all the names declared in current scope like function names, variable names, module name, built-in names

# Built - in namespace : dir() -> interpreter
list of :  ['__annotations__', '__builtins__', '__cached__', '__doc__', '__file__', '__loader__', '__name__', '__package__', '__spec__']

--> print(), int(), len() etc

# Module level / global namespace
When you run any particular python module during that time global / module level namespace gets created in context with particular module

It helps us to declare same function/variable/class names, avoiding any conflict while calling it at same time.

# Local namespace:
It gets created for function and classes.

# Enclosed namespace:
Its a namespace for nested function

# -------------------------------------

# global : used with outer function body : Local scope
# nonlocal : used with nested function body : Enclosed scope

# -----------------------------------
# Closure: 
it is a techniques by which data gets attached to the code.
- Closures are function objects that remembers values in the enclosing scope even if they are not present in the memory

Closures are possible because of conditions like:
1. Nested function.
2. Outer function should return inner function

# ----------------------------------
# Decorator function:
They add extra functionalities with existing functions
OCP: Open/Closed Principle: Close to modification but ope for extension




